<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.uninsubria.pdm.money">

    <application
        android:name=".Home"
        android:allowBackup="true"
        android:icon="@drawable/dollar_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dollar_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditMovimento"></activity>
        <activity android:name=".FiltraMovimenti" />
        <activity android:name=".ListViewMovimenti" />
        <activity android:name=".Registrazione">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AggiungiMovimento" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>